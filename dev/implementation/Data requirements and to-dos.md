# Data requirements and to-dos

This is the backend-facing home for implementation hints. Product-facing flow docs should name user-visible data, decisions, and failure modes first; this file is compiled from those product docs after the flow shape is settled.

User preference / thesis persistence is outside the Gearbox-side scope. Rows that mention user policy inputs describe runtime inputs supplied by the user or the user's representative agent, not data Gearbox must store.

| Area | Description | Notes |
| --- | --- | --- |
| **Stage handoff artifact vocabulary.** | Implementation names for canonical-loop handoffs: `OpportunityCandidate`, `ResearchMemo`, `ProposedAction`, `TransactionPreview`, `ExecutionReceipt`, `MonitoringSnapshot`, and `MonitorAlert`. Flow docs own the user-visible `Inputs / Compute / Outputs`; implementation should keep only the minimal object needed by the next stage. | Source labels per field: protocol, indexer, issuer / compliance, product policy, user / agent policy. |
| **Source provenance and unknown-state labels.** | Any field used in a verdict, ranking, Preview, or automation decision must separate protocol facts, indexer facts, issuer / compliance facts, product judgment, and user / agent policy. | Missing external data should surface as `unknown` or `blocking` where material, not as green status. |
| **Curator profile endpoint.** | A standalone `CuratorProfile` is called out across Stage 1, Stage 2a Q4, and Stage 2b Q3. Today it is missing. Shipping this unblocks "Who manages this pool?" and "Who manages this strategy?" on both flows. | ==note: and/or external linkl + summary from DefiLlama or similar== |
| **Parameter-change log + pending governance feed.** | Both flows (Analyze Q5-LP, Analyze Q4-CA, Monitor 6a, Monitor 6b) rely on the same `EventFeedItem` / `GovernanceChange[]` machinery. One stream, many readers. | |
| **Historical series (7 series).** | Supply rate 90d, incentive 90d, composite APY 90d, utilisation 90d, TVL 90d, borrow rate 30d, volatility 90d, oracle prices 90d, share price 90d, price-impact 90d. Backend needs to store and serve these daily. | |
| **Preview type family and integrity gate.** | `TransactionPreview`, `PreviewRoute`, and `RawTx` are missing. Preview needs `preview_id`, expiry, pass/fail result, warning severity / source / blocking status, approval mode, exact `execution_package`, and integrity binding so Execute can prove it is submitting the approved package. Today Preview is the weakest stage in terms of served fields — which is the highest-leverage one to harden because it's the execution gate. | Applies to emergency and RWA / compliance-gated actions too. |
| **RWA / KYC extension (`RwaAssetProfile`, `RwaComplianceProfile`).** | 32 additional fields across the stages. Needed end-to-end before the Securitize-integrated CMs are safe to promote in the UI. | |
| **PnL / returns endpoint.** | Flagged as the single largest information gap by the UX audit. Requires: account value history (per-tx or daily), cost-basis anchor at entry, yield-source decomposition (farming / rewards / price appreciation / borrowing cost / protection-bot fees), merkle-reward attribution (rewards accrue to the owner wallet, not the CA — need explicit linkage for PnL totals). | |
| **Before/after transaction preview component.** | Backed by the `TransactionPreview` family above, but the component itself — two-column current → projected for HF, leverage, equity, position size, net APY — is a first-class product surface that every action flow (Add Collateral, Reduce Leverage, Increase Leverage, Enter Farm, Partial Withdrawal, Close) consumes. | ==note: maybe it's on the wallet side though?== |
| **Scenario simulator + virtual liquidation counter.** | Both are backend-dependent risk-analysis primitives that surface the same underlying facts already needed for Monitor stage, but in a "what if" frame. The virtual-liquidation counter (how many times the position would have been liquidated in the past 90 days based on historical price action) is a high-trust primitive that competitors don't offer. | |
| **Contextual recommendation engine.** | Quick-Actions logic must be parameterized by user policy and asset-specific blockers rather than fixed universal thresholds. Examples: Add Collateral / Reduce Leverage when the user safety floor is breached or projected to breach; Claim when claimable rewards or matured withdrawals matter; suggest switch when another strategy better satisfies the user mandate. | ==note: don't forget that there are current recommendations for better strategies if present + for specific reward / withdrawal events== |
| **Insurance-fund balance delta feed.** | Time-series of insurance fund balance plus delta vs last check / rolling history. Required by `Pool monitoring` Q5 (bad-debt canary) and `Pool deposit` Q2 (exposure trace). | |
| **Curator bad-debt tolerance policy input.** | User / mandate policy for how much cumulative curator bad debt is acceptable before a deposit is considered out of mandate. Needed by `Pool deposit` Q4 curator diligence. Product may suggest a policy, but the agent should not apply a hidden default. | |
| **User / agent-supplied concentration policy.** | Runtime policy input with two values: (a) max % of user portfolio in any single pool, (b) max % of any single pool's TVL the user is willing to occupy. Needed by `Pool deposit` sizing gates and `Pool monitoring` top-up / exit actions. | User-side persistence; Gearbox consumes it only as supplied policy input. |
| **Strategy plain-language description.** | Curator-supplied or platform-curated human-readable description per strategy, surfaced verbatim in the CA monitoring view. Authorship policy (curator vs platform) needs a decision. Needed by `Credit Account management` Q3. | |
| **Issuer-controlled collateral eligible-liquidator depth feed.** | Eligible-liquidator count / depth for issuer-controlled collateral. The interpretation depends on user or product policy; the feed itself should not encode a universal threshold. Needed by `Pool deposit` issuer-controlled risk checks and `Credit Account opening` / `Credit Account management` conditional collateral branches. | |
| **Per-token oracle staleness feed.** | Per-token freshness (last-update timestamp + staleness window) and main-vs-reserve divergence series. Required by `Pool monitoring` Q6 and `Credit Account management` Q6. | |
| **APY / rewards / yield-composition feed.** | Pool and strategy opportunity fields for organic APY, incentive layers, expiry, reference URL, top-up history, renewal schedule, reward materiality, and composite APY breakdown. | Backend/indexer feed; product copy should distinguish confirmed organic yield from incentive assumptions. |
| **Governance / config event feed shape.** | Indexer-backed change feed with parameter changed, effective time, timelock / Safe status, materiality, market / pool / Credit Manager scope, and 30-day window support. | Extends the existing parameter-change row with the fields needed to classify material vs cosmetic changes. |
| **Market risk history.** | Cumulative bad debt, bad-debt incidents, liquidity incidents, AUM, utilisation history, withdrawal pressure, and historical pool/debt composition. | Needed for curator track record, exit confidence, and bad-debt canary explanations. |
| **User / agent policy inputs.** | User APY floor, thesis, accepted oracle methodology, leverage tolerance, position-sizing preference, hold horizon, concentration caps, and notification/automation preferences. | Runtime policy input; not a protocol field and not Gearbox-side persistent state. |
| **RWA issuer-state feed.** | Frozen-account count, aggregate frozen debt, KYC/whitelist validity, eligible liquidator count, redemption calendar, issuer status, and issuer/regulatory event notes. | Issuer/indexer source; Gearbox state alone is insufficient for the product verdict. |
| **Oracle telemetry + methodology feed.** | Current oracle category/methodology, last update, staleness window, main-vs-reserve divergence, methodology-change history, and source confidence. | Backend hint: accepted methodology remains product/user policy; raw updates may come from oracle-specific payloads such as `PriceUpdate({priceFeed, data})`. |
| **Withdrawal queue / claim-readiness feed.** | Supported withdrawal assets, per-account pending/claimable queues, `claimableAt`, expected amount, claim readiness, and blocking reason. | Backend hint: periphery/SDK surface includes `getWithdrawableAssets(creditManager)` for supported assets and `getCurrentWithdrawals(creditAccount)` for current pending/claimable queue state. Keep these names out of product-facing flow copy. |
| **LT-ramp schedule fields.** | Current LT, target LT, ramp start, ramp duration, direction, and projected effective LT at the user's hold horizon. | Backend hint: map from implementation fields such as `ltInitial`, `ltFinal`, `timestampRampStart`, and `rampDuration`; product docs should say “LT-ramp schedule.” |
| **KYC-gated execution routing metadata.** | For compliance-gated CMs, expose whether the account requires compliance-gated execution, whether bot delegation is blocked, which user action path is allowed, and the failure reason when KYC/freeze/investor-registry checks block execution. | Backend hint: contract-level routing may involve `SecuritizeKYCFactory`, `SecuritizeWallet`, and `CreditFacade`; product docs should surface this as “compliance-gated execution” and HITL-only management. |
